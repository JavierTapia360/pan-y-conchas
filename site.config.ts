export const siteConfig = {
  brandName: 'CUATESFARMZ',
  defaultLocale: 'auto',
  supportedLocales: ['es', 'en'],
  ageGate: 21,
  features: {
    admin: true,
    merch: true,
    cannabisCommerce: false,
    newsletter: true,
  },
} as const;
